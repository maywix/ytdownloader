services:
  ytdown:
    build: .
    container_name: ytdown
    ports:
      - "8080:8080"
    volumes:
      # Les fichiers téléchargés survivent aux redémarrages
      - ./downloads:/app/downloads
    restart: always
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
